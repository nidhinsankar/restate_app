import { Text, View } from "react-native";

const Card = () => {
  return (
    <View className="py-4 bg-red-300 text-blue-900">
      <Text className="text-bold text-blue-300">card</Text>
    </View>
  );
};

export default Card;
